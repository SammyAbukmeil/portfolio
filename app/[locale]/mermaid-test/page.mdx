import Mermaid from "@/app/components/blog/Mermaid";

# Replication

<Mermaid
  chart={`
flowchart LR
  W[Write request] --> L[Leader]
  L -.->|replication lag| F1[Follower 1]
  L -.->|replication lag| F2[Follower 2]
  F1 --> R1[Read request]
  F2 --> R2[Read request]
  L --> R3[Read your own writes]
`}
/>

# Sharding

<Mermaid
  chart={`
flowchart LR
  K1[user:alice] --> H{{hash mod 3}}
  K2[user:bob] --> H
  K3[user:carol] --> H
  H --> S0[(Shard 0)]
  H --> S1[(Shard 1)]
  H --> S2[(Shard 2)]
`}
/>

# Queues

<Mermaid
  chart={`
flowchart LR
  U[User uploads] --> API[Web server]
  API -->|202 accepted| U
  API --> Q[[Job queue]]
  Q --> W1[Worker 1]
  Q --> W2[Worker 2]
  Q --> W3[Worker 3]
  W1 --> DB[(Database)]
  W2 --> DB
  W3 -.->|failed 3 times| DLQ[[Dead letter queue]]
`}
/>
